# 소유권 · 라이선스 · 비종속 (Ownership / License / Non-dependency)

> ⚠️ 이 문서는 **의도·설계 원칙의 명문화**이며 법적 계약을 대체하지 않는다. 실제 권리·의무의 SoT = 별도 서면 계약.

---

## 1. 소유 (Ownership)

- `naia-kb-compiler` 엔진 = **넥스테인(Nextain) 소유 자산**(배경 IP / background IP).
- 본 엔진은 **독립 자산**으로, 이를 소비하는 응용과 **레포·소유가 분리**된다.
- 프로젝트 용역비는 **응용(키오스크 등) 납품 대가**이며, **범용 엔진의 양도 대가가 아니다.** (foreground 산출물 ≠ background 엔진)
- 발명·저작 시점 증거 = 본 넥스테인 레포의 커밋 이력(provenance).

## 2. 비종속 / 교체 가능성 (Non-dependency / Exit) — 본 레포의 설계 의무

어떤 소비자도 **언제든 본 엔진을 다른 솔루션으로 교체할 수 있어야 한다.** 이는 신뢰의 표현이자, 종속을 만들지 않겠다는 **설계 약속**이다. 법적 문구가 아니라 다음으로 *기술적으로 보장*한다:

1. **데이터 가반성(portability)**: 지식 정본(엔티티·관계·서비스카드·운영자 정답셋)은 **표준·문서화된 포맷**(JSON/관계형)으로 저장하고 **무손실 export**를 제공한다. 매니지드 인덱스(벡터·그래프)는 *재생성 가능한 캐시*일 뿐 정본이 아니다.
2. **port/adapter 경계**: 모든 외부 의존(LLM·임베딩·검색 백엔드·저장소·파서)은 포트 뒤 어댑터. 소비자는 본 엔진을 빼고 자체/타사 구현으로 대체 가능.
3. **표준 인터페이스**: 엔진의 입출력(자료 투입 → 검증된 KB·grounded 응답)을 문서화된 API로 노출 → 동등 대체 구현이 가능.
4. **종속 유발 설계 금지**: 독점 포맷·비공개 잠금·계약상 탈출 방해 장치를 두지 않는다.

→ 결과: **소비자도, 넥스테인도 한 엔진/한 백엔드/한 provider에 갇히지 않는다(양방향 비종속).**

## 3. 기업·파트너 권리

> 특정 기업·파트너에 대한 사용·재판매 권리와 조건은 **별도 서면 계약**으로 정한다.

## 4. 한 줄 요약

> 엔진은 **넥스테인 소유**, **누구도 이 엔진/백엔드에 종속되지 않도록 설계로 보장**한다. 기업·파트너 권리는 별도 계약.
